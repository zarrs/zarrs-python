name: zarrs-python

on:
  push:
      branches:
        - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env: 
  CARGO_TERM_COLOR: always

jobs:
  build_and_test:
    name: zarrs-python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install UV
        uses: hynek/setup-cached-uv@v2
        with:
          cache-dependency-path: pyproject.toml
      
      - name: Install python deps
        run: |
          uv pip install --system -e ".[test]" maturin --verbose
      
      - name: Build
        run: |
          env VIRTUAL_ENV=$(python3 -c 'import sys; print(sys.base_prefix)') maturin develop --uv --skip-install
      
      - name: Test python
        run: pytest zarrs_python/tests/v3              
